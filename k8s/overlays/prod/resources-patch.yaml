apiVersion: apps/v1
kind: Deployment
metadata:
  name: bookstore
  namespace: bookstore-prod
spec:
  template:
    spec:
      containers:
        - name: bookstore
          resources:
            requests:
              cpu: 250m
              memory: 256Mi
            limits:
              cpu: 1000m
              memory: 512Mi
